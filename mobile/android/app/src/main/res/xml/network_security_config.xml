<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (http/ws) is only allowed for the Android emulator's loopback
  alias (10.0.2.2) so you can hit a backend running on your dev machine
  during development. Everything else must use HTTPS/WSS. Before release,
  make sure API_BASE_URL in src/config/env.ts points at an https:// host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
